using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class Tile_Counter : MonoBehaviour
{

    //[SerializeField] private Tilemap groundTilemap;

    /// <summary> Get the amount of tiles controlled by a player </summary> 
    //public int GetTileAmountSprite(Sprite targetSprite) 
    //{
    //    int amount = 0;

    //    // loop through all of the tiles        
    //    BoundsInt bounds = tilemap.cellBounds;
    //    foreach (Vector3Int pos in bounds.allPositionsWit$$anonymous$$n)
    // {
    //        Tile tile = tilemap.GetTile<Tile>(pos);
    //        if (tile != null)
    //        {
    //            if (tile.sprite == targetSprite)
    //            {
    //                amount += 1;
    //            }
    //        }
    //    }

    //    Debug.Log(amount);
    //    return amount;
    //}

    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //}
}
